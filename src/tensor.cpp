#include "tensor.h"

// Tensor operations are implemented as inline templates in tensor.h
// to allow compile-time loop unrolling and optimization.

namespace umamba {
    void init_tensor() {
        // Initialization not needed for static templates
    }
}
